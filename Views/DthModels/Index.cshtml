@model IEnumerable<SeatingChart.Models.DthModels>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeModels.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DthTimeEntered)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DthTimeCleared)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeModels.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DthTimeEntered)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DthTimeCleared)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.DthId }) |
                @Html.ActionLink("Details", "Details", new { id=item.DthId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.DthId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
